[package]
name = "wave-experiments"
version = "0.1.0"
edition = "2024"
description = "Pure-math wave coherence experiments in Rust: DFT wave packets, harmonic construction, musical interval theory. No GPU, no external dependencies."

[dependencies]
